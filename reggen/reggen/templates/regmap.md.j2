# {{ block.name }} register map

<!-- GENERATED by reggen. Do not edit by hand; edit the YAML spec and regenerate. -->
{% if block.description %}

{{ block.description }}
{% endif %}

| Property | Value |
|---|---|
| Data width | {{ block.data_width }} bits |
| Address width | {{ block.addr_width }} bits |
| Base address | {{ block.base_address | hex(block.addr_width) }} |

## Registers

| Offset | Name | Reset | Description |
|---|---|---|---|
{% for r in registers %}
| {{ r.offset | hex(block.addr_width) }} | [{{ r.name }}](#{{ r.name | lower }}) | {{ r.reset_value | hex(block.data_width) }} | {{ r.description }} |
{% endfor %}

{% for r in registers %}
### {{ r.name }}

**Offset:** {{ r.offset | hex(block.addr_width) }} &nbsp;&nbsp; **Reset:** {{ r.reset_value | hex(block.data_width) }}
{% if r.description %}

{{ r.description }}
{% endif %}

| Bits | Field | Access | Reset | Description |
|---|---|---|---|---|
{% for f in r.fields %}
| {{ f | bits }} | {{ f.name }} | {{ f.access.name }} | {{ f.reset | hex(4) }} | {{ f.description }} |
{% endfor %}
{% for f in r.fields %}
{% if f.enums %}

*{{ f.name }} values:*
{% for e in f.enums %}
- `{{ e.value }}` — **{{ e.name }}**{% if e.description %}: {{ e.description }}{% endif %}

{% endfor %}
{% endif %}
{% endfor %}

{% endfor %}
