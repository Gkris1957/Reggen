"""Output generators (SystemVerilog, UVM, C, Markdown).

Populated from M3 onward. Each generator consumes the validated spec / IR and
emits one artifact. Kept empty in M1 so the package imports cleanly.
"""
